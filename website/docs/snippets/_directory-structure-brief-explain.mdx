Though seemingly many files, it is pretty simple:
"a standard Flutter app + a standard Rust crate + some glues to ignore".
In short, write Flutter near `lib/main.dart` and Rust near `rust/src/api/simple.rs`.
